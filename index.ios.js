import React from 'react';
import { AppRegistry } from 'react-native';
import App from './app/App';

const WorkingDays = () => <App/>;

AppRegistry.registerComponent('WorkingDays', () => WorkingDays);
